#!/usr/bin/perl -w

# given a set of files, tries to determine from where they came.

# method 1 look at all URLs contained in the archives.  If a given
# file has too many URLs, disregard them.

# method 2
# do website search for each of the files.
